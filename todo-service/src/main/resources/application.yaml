spring:
  camunda.bpm:
    admin-user:
      id: demo
      password: demo
      firstName: Demo
    filter:
      create: All tasks
  datasource:
   driverClassName: org.h2.Driver
   username: sa
   password:
   url: jdbc:h2:mem:todo
  jpa:
   database-platform: org.hibernate.dialect.H2Dialect
